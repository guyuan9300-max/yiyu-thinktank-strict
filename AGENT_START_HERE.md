# Agent 接手必读（正式软件仓库）

> 这是本仓所有新 Agent 的统一入口。先读完本文件，再分析、设计或修改代码。
>
> 本文件说明的是：**仓库身份、产品意图、UI 规则、88 表数据高速公路、验证标准和交接方式**。它不是“功能已经全部接通”的声明。

## 0. 先确认你进入了正确仓库

- 正式仓库：`/Users/guyuanyuan/Desktop/0.29.2/益语智库AI新版-0.29.2-源码`
- Git 远端：`https://github.com/guyuan9300-max/yiyu-thinktank-strict.git`
- 正式开发分支：`main`
- 本仓是“严格新版 / 蓝图 88 表”根历史。
- UI 创新沙盘、网页演示、Excalidraw 和本地模拟数据都不是正式运行证据，也不能直接替代本仓代码。

开始工作前必须记录：

```text
仓库绝对路径：
当前分支：
HEAD：
工作区已有改动：
本次任务边界：
```

如果路径、远端或分支不一致，先停止修改并说明。不要把旧仓、安装包内源码、DMG、UI 沙盘或临时副本当成正式仓库。

## 1. 必读顺序

1. [AGENTS.md](AGENTS.md)：本仓最高优先级工程禁令。
2. [README.md](README.md)：88 表严格新版的仓库定位、启动和验收边界。
3. [contracts/STRICT_PHYSICAL_SCHEMA_CONTRACT_V7.md](contracts/STRICT_PHYSICAL_SCHEMA_CONTRACT_V7.md)：物理架构合同。
4. [contracts/strict-local-schema-manifest.v1.json](contracts/strict-local-schema-manifest.v1.json)：本地 88 表冻结清单。
5. [contracts/strict-cloud-schema-manifest.v1.json](contracts/strict-cloud-schema-manifest.v1.json)：云端 88 表冻结清单。
6. [contracts/LEGACY_MIGRATION_POLICY.md](contracts/LEGACY_MIGRATION_POLICY.md)：旧库、迁移和回退边界。
7. 与任务直接相关的入口、共享层、领域服务、权威仓储和测试；不要一开始全仓盲扫。

清单 JSON 和集中 schema builder 才是 88 表的精确权威。本文件不复制第二份表名清单，避免文档与真实 schema 漂移。

## 2. 产品地图：五个主功能区

### 2.1 工作台：个人工作的统一入口

目的：把个人文件、客户/项目资料、问答、编辑和 AI 工作集中在同一上下文中。

- 客户与项目工作空间：确定当前组织、客户、项目和资料范围。
- 文件与资料：导入、解析、搜索、打开原文、回收、归档和状态展示。
- AI 问答：根据明确范围检索资料、形成回答，并提供可追溯引用。
- 智能编辑：对文档进行局部或整体加工，保留用户确认权。
- 工作模式：把自然语言目标转为可审核的步骤、工具调用和结果。
- 结果流转：可转为任务、计划、决策或正式文档，但必须经过对应领域命令，不能由前端直接写库。

主入口从 [src/renderer/App.tsx](src/renderer/App.tsx) 开始；共享请求与 AI 命令分别优先检查 [src/renderer/lib/api.ts](src/renderer/lib/api.ts) 和 [src/renderer/lib/aiCommand.ts](src/renderer/lib/aiCommand.ts)。

### 2.2 任务与日程：个人与组织执行中心

目的：让计划、任务、会议、日历、事件线和复盘形成同一条执行链。

- 协作收件箱：等待本人确认、认领或处理的协作事项。
- 任务列表：从权威任务事实生成的视图，不是第二套任务数据。
- 我的月历：按负责人、协作者和时间范围投影任务与日程。
- 组织计划：组织目标、部门计划、计划项、负责人、进度和关联任务。
- 事件线：重要事实、会议、决策和执行变化的时间序列。
- 周复盘：按真实任务、会议和计划进展生成可核查总结。

计划与任务是多对多关系；计划开始时间、任务截止时间和日历展示时间不得混为一个字段。相关界面可从 [src/renderer/components/plan_workshop/PlanWorkshopView.tsx](src/renderer/components/plan_workshop/PlanWorkshopView.tsx) 与 [src/renderer/components/tasks/WeeklyReviewSummaryPanel.tsx](src/renderer/components/tasks/WeeklyReviewSummaryPanel.tsx) 追踪。

### 2.3 战略陪伴：证据驱动的经营与战略视角

目的：基于组织权威事实持续给管理层提供判断，而不是生成一份静态报告。

- 客户档案与组织叙事：由事实、资料、会议、计划和版本化结论重建。
- 判断与思考：区分事实、变化、判断、行动建议和待确认项。
- 经营驾驶舱：指标、趋势、异常、项目健康度和管理层待决策事项。
- 战略内容：必须关联当前有效资料、计划、日程和会议证据，不能凭空补全人物或机构事实。
- 叙事迭代：新证据进入后触发可重建更新；不能把旧答案当永久权威。

相关界面从 [src/renderer/components/strategic_accompaniment/StrategicBrainView.tsx](src/renderer/components/strategic_accompaniment/StrategicBrainView.tsx) 开始追踪。

### 2.4 资讯情报站：内外部变化与风险预警

目的：把可靠的外部信息和内部变化转成可行动信号，而不只是新闻阅读页。

- 来源与可信度：保存来源身份、抓取/核验时间和冲突状态。
- 外部变化：市场、供应商、品牌、政策、合规、合同和竞争信息。
- 内部风险：到期、义务、价格、库存、履约和异常信号。
- 事实补偿：互联网信息只能形成候选事实；经来源比对与核验后才可进入共享可信事实主干线。
- 行动闭环：达到规则阈值后可发起分析、任务或决策，但不能绕过权限与人工确认边界。

### 2.5 成长中心：经验、能力与贡献的可重建投影

目的：让个人和团队的真实工作证据沉淀为组织资产，不建立第二套绩效数据库。

- 工作成果与贡献证据。
- 项目经验、复盘与可复用方法。
- Skill 与人机协作能力记录。
- 跨项目的长期贡献轨迹。
- 所有成长结论必须能回到任务、文档、会议、决策或交付证据。

## 3. 共享基础能力，不另造页面和数据孤岛

- 组织、成员、角色和权限。
- 客户、项目、任务、计划、会议、日历和事件线。
- 文件、版本、内容块、引用、解析、检索和知识投影。
- AI 调用、模型配置、工具权限、执行回执和成本记录。
- 同步、Outbox/Inbox、幂等、重试、撤销、恢复和审计。
- 供应链、会员、合同和合规属于能力包；通过五大主功能区使用，不为每个能力包再造一套孤立前台。

## 4. UI 设计规则

### 4.1 总原则

- 延续原产品：克制、可信、留白充足、层级明确、轻智能感。
- 信息优先于装饰；一个页面只突出一个主任务。
- 先复用既有布局、组件、间距和交互，再新增模式。
- 中文优先，用户只看到业务语言；不得暴露表名、枚举、内部状态码或调用结构。
- 任何智能效果都必须对应真实运行状态，不能用动画掩盖等待，也不能展示虚假的“思考过程”。

### 4.2 视觉约束

- 字体优先：`PingFang SC`、`SF Pro`、`Helvetica Neue`。
- 页面标题约 26–30px，正文 14–15px；标题、结论、证据、操作形成清晰层级。
- 主色使用益语蓝（参考 `#5B7BFE`）；一个区域只保留一个实心主按钮。
- 状态色只表达状态：绿=成功，橙=提醒，红=失败/风险，灰=中性/不可用。
- 采用 4px 间距体系；内容区安全留白通常 32–48px；卡片圆角约 12px。
- 用留白、细分割线和浅背景组织内容；避免大面积深色、炫光、强渐变、重阴影和装饰性动效。

### 4.3 交互与状态

统一状态至少包括：

```text
idle / loading / refreshing / success / empty /
failed_retryable / blocked / cancelled / partial_success
```

- `loading` 不应清空已有内容；`refreshing` 保留旧内容并显示轻量更新状态。
- 超时、弱网和部分成功必须准确显示，不能伪装成空数据、成功或永久失败。
- 同一次用户写操作必须复用同一幂等键；超时后恢复原回执，不能重复创建。
- 进度只能前进或进入明确终态，不能反复归零、闪烁或出现多个相互矛盾的进度卡。
- 切换组织、sandbox、项目或页面后，迟到回包不得更新新的界面作用域。
- AI 过程只显示可验证阶段，如“正在读取已授权资料”“正在核对引用”“等待确认”；不得显示或伪造隐藏推理。

### 4.4 页面检查

每次 UI 修改至少检查：

- 长中文、窄窗口、缩放和滚动。
- 加载、刷新、空、失败、重试、阻断和部分成功。
- 键盘焦点、按钮禁用原因和可恢复路径。
- 页面是否仍沿用当前产品结构，而不是另造一套设计语言。
- 用户是否能看懂“发生了什么、为什么、下一步做什么”。

## 5. 88 表与“数据高速公路”规则

### 5.1 88 表是什么

- 本地与云端各有同名的 88 张标准表，并由冻结 manifest、schema builder 和校验测试共同约束。
- “有 88 张表”只证明物理表面存在，不证明业务功能已接通。
- 精确表名、字段、索引和约束只从 manifest 与 [strict_common/physical_schema.py](strict_common/physical_schema.py)、[strict_common/schema.py](strict_common/schema.py) 读取。
- 不允许在页面、路由、服务或文档中维护另一份手写 schema 真相。

### 5.2 高速公路的核心不变量

- 一个事实只允许一个权威来源；其他搜索、AI、看板和列表都是可重建投影。
- 每个业务对象必须有稳定 ID、组织/sandbox 作用域、生命周期、版本、权限和审计身份。
- 本地业务归属只认 `sandbox_id`；组织身份只认 `cloud_instance_id + organization_id`。
- Renderer 和 Route 不得直接执行 SQL；写入必须经过版本化命令、领域规则、事务和权威仓储。
- 副作用必须进入持久任务、Outbox/Inbox 或可追踪回执；不能依赖进程内线程完成关键业务。
- 搜索、AI、引用、看板和成长结论不得成为业务权威，也不得反向覆盖权威事实。
- 删除、撤销、权限收回和恢复必须同步传播到引用、搜索、AI 和投影。

标准链路：

```text
前台/后台触发
→ 身份与作用域校验
→ 版本化命令或查询
→ 唯一权威对象与稳定 ID
→ 领域约束、状态机、权限与 CAS
→ 事务、幂等与持久任务
→ Outbox/Inbox/Saga 或外部权威回执
→ 可重建投影、检索、AI 与前台状态
→ 审计、撤销、恢复和可重放证据
```

### 5.3 典型语义边界

- 任务列表是同一任务事实的投影，不是第二套任务库。
- 任务与计划是多对多关系；标题变化不能破坏关联。
- 工作台未关联的个人文档仍是本地草稿，不能自动成为组织 AI 上下文。
- “纳入项目数据中心”和“将原始文件同步给团队”是两个独立授权动作。
- 战略陪伴是持续证据链上的判断投影；成长中心是工作证据的投影。
- 互联网搜索结果、模型输出和人工草稿先作为候选事实，核验后才能提升为共享可信事实。

### 5.4 绝对禁止

- 读取旧数据库路径、调用 `/api/v1`、旧表 fallback、新旧双读或双写。
- 为某个页面新增旁路表、临时权威、匿名字符串关联或第二套状态机。
- 在运行时代码执行 DDL；DDL 只允许集中 schema builder 执行并更新冻结 manifest。
- 把投影、缓存、搜索结果、AI 答案或蓝图当成权威数据。
- 把 HTTP 200、表存在、单元测试、截图、模拟数据或构建成功说成真实业务已经接通。
- 未接通时返回假空数据；必须返回准确的 `not_connected` 或对应失败状态。

## 6. 修改一项功能时的工作方法

### 6.1 先冻结业务合同

在写代码前回答：

1. 用户动作和成功终态是什么？
2. 权威对象和稳定 ID 是什么？
3. 谁可读、谁可写、作用域是什么？
4. 状态机、版本/CAS 和幂等键是什么？
5. 本地与云端各自负责什么？
6. 断网、超时、重试、撤销、删除、恢复如何处理？
7. 哪些只是投影，如何重建？
8. 前台怎样准确显示五类以上状态？

如果这些问题没有答案，不要先加表或接旁路。

### 6.2 追踪完整纵向链

```text
真实点击
→ Renderer 请求
→ 本地 /api/v2
→ 共享请求调度
→ 领域服务/权威仓储
→ 本地事务或组织云命令
→ 回执与投影刷新
→ 页面终态
```

只修找到的断点；通用问题修在共享层，不在每个页面复制重试、轮询、幂等和作用域逻辑。

### 6.3 证据等级

从弱到强：

1. 设计稿、蓝图、说明文档。
2. 静态代码和 manifest 合同。
3. 单元/集成测试。
4. 本地真实前台纵向链。
5. 安装后的真实软件 + 真实账号/组织云 + 重启/弱网/并发/恢复验证。

只有第 5 级才能声明“真实接通”。较低等级只能说明设计、合同或候选实现已经完成。

## 7. 提交前最低验收

- 未覆盖用户已有工作区改动；差异只包含本任务文件。
- 新增或改变 schema 时，manifest、schema builder、合同和测试同步更新。
- 真实入口能够到达正确终态，不只验证 HTTP 200。
- 重复点击、并发、超时重试和崩溃重启不重复写入。
- 权限、组织/sandbox、对象 ID 和请求序号在回包时再次校验。
- 删除、撤销、恢复和权限收回能够传播到所有投影。
- 执行仓库要求的 `npm run audit:strict`、`npm run test:strict` 和适当构建；若全量门禁存在既有失败，分别报告本次定向结果和全量结果。
- 若交付安装包，必须真实安装、启动和冒烟验证；开发候选不能冒充正式发布。

## 8. Agent 交接格式

每次结束时在任务回复或独立交接文件中留下：

```text
目标：
正式仓库 / 分支 / HEAD：
用户可感知变化：
权威对象与数据链：
修改文件：
未修改的既有脏文件：
验证命令与结果：
真实运行证据：
尚未验证 / 风险：
下一步唯一建议：
```

不要写“应该可以”“大概率通过”作为验收结论。清楚标明：**已实现、已测试、已在真实软件验证、尚未验证**。

---

接手本仓的首要职责不是快速增加页面，而是让每个功能沿同一条数据高速公路稳定运行：**唯一权威、明确作用域、持久执行、可重建投影、准确状态、可追溯证据、不双写、不回退。**
