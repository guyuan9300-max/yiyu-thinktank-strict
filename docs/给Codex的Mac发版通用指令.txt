在当前“益语智库AI严格新版”仓库完成一次正式 Mac 发版，新版本号为【在这里填写版本号】。

请严格按固定顺序执行：
1. 先确认当前仓库是 guyuan9300-max/yiyu-thinktank-strict 的 main，且没有未解决冲突；不要使用旧仓、旧数据库或旧接口。
2. 用 npm 同步更新 package.json 和 package-lock.json 的版本号，不要只改界面显示。
3. 将本阶段应交付的源码修改纳入严格审计和构建；如果发现88表、仓库身份、安装包白名单或构建错误，先修复再继续。
4. 先提交并推送远端 main，确认远端 main 与本机提交完全一致；远端有新提交时只允许安全接收，禁止强制覆盖。
5. main 推送确认成功后，才打包 Mac 签名 DMG。签名必须从当前 Mac 钥匙串自动取得有效的 Developer ID Application 证书和私钥；不要把签名人写死在临时指令里，也不要把证书或密码写入仓库。
6. 最终 DMG 必须使用“安装或更新益语智库AI”安装器：用户双击、确认后，安装器自动退出旧版、找到扩展坞实际使用的同 Bundle ID 软件、原位完整替换并打开新版；禁止退回拖拽到 Applications 的旧结构。
7. 打包后必须在隔离目录用一个同 Bundle ID 的旧版副本真实跑一次覆盖安装，确认版本号已变为本轮新版本、签名正确、旧内容已消失、没有 previous/update 临时副本；不得只检查 DMG 能打开或 App 能启动。
8. 将最终 DMG 放到桌面以版本号命名的文件夹，报告远端 main 提交、DMG 路径、签名状态、覆盖验证结果和 SHA-256。除非我明确要求，不要覆盖安装本机现有软件。

仓库已提供通用命令：
npm run release:mac-signed -- 【新版本号】 "$HOME/Desktop/【新版本号】"

如果是某个相关文件的一次性异常，先说明并暂停；不要把一次性文件处理写进通用发版流程。
