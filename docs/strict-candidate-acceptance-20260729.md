# 严格新版候选验收记录

日期：2026-07-29

## 候选边界

- 本仓拥有独立 Git 根历史，不继承旧仓提交。
- 本地业务库只允许 manifest 中的 23 张表。
- 云端业务库只允许 manifest 中的 38 张表。
- 本地 schema：`yiyu-strict-local-v1`。
- 云端 schema：`yiyu-strict-cloud-v1`。
- 本地 manifest SHA256：`4498c8e44b20199603dd1e452ed2dfdd34604efa34eeccd1c880d00e0d4f36a8`。
- 云端 manifest SHA256：`47a463a7e74916fac71bf0239127f6c8b75451e700d11abfa4c84b3e91ebfbf8`。
- 运行时业务接口只使用 `/api/v2`。
- 旧数据库路径、旧 API、旧代理和旧 active workspace helper 均被静态审计与打包审计阻断。

## 已接通范围

- 设备与工作空间。
- 云实例、组织、账号、成员、部门、权限与会话。
- 组织统一 AI 配置与系统密钥库。
- 版本/CAS、幂等、outbox、审计和恢复底座。

其余业务入口保留，但明确显示 `not_connected`，不得读取旧表或把失败显示为空。

## 旧版界面结构对照

- 任务与日程：顶部切换协作收件箱、任务列表、我的月历、组织计划、事件线和周复盘；右侧保留新建任务与 AI 动作。
- 工作台：左侧项目、中间智能问答/编辑、右侧文件/收藏/工具三栏。
- 战略陪伴：顶部切换客户档案与判断/思考；事件线不再误放在该模块。
- 资讯情报站：顶部切换品牌监测与时效情报。
- 成长中心：顶部切换经验墙、能力成长与徽章/排行。
- 系统设置：保留独立左侧设置导航。

旧版仅作为信息架构与视觉参考；没有复制旧数据库查询、缓存、权限或同步代码。

## 机械验收

- `npm run test:strict`：Python 9 项、renderer 3 项通过。
- `npm run audit:strict`：五类严格审计通过。
- `npm run build`：主进程、renderer、Python 和冻结后端通过。
- `npm run package:mac-local`：本地 unsigned App 候选构建通过。
- `npm run test:packaged`：应用身份、资源边界、asar 边界和旧线路毒化检查通过。
- 打包 App 使用独立临时数据目录启动：
  - `/api/v2/health` 返回 `yiyu-strict-local-v1`。
  - 当前空间为 `local_draft`，数据库代次存在。
  - App 内只登记严格新版能力。
- 真实前端逐项点击 21 个模块/子视图，无控制台错误。
- 打包 App 在 1080px 普通窗口与 1440px 宽窗口间往返调整后，任务、工作台、战略陪伴、资讯情报站、成长中心和系统设置均铺满剩余主区域。
- 1440px 和应用最小 1080px 宽度无页面级横向溢出。

## 当前候选

本地测试 App：

`dist/mac-arm64/益语智库AI（新版）.app`

该 App 未签名、未公证、未覆盖安装、未连接真实组织云，仅用于阶段二候选验收。
