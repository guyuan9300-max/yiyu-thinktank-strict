# v4 运行时冻结区

这里保存 2026-08-04 切换 88 表底座前的本地 24 表、云端 81 表运行时代码、静态创世 SQL 和迁移证据。

硬边界：

- 正式入口不得导入本目录。
- 活动数据库不得 `ATTACH` 或读取 v4 归档库。
- 本目录不得打入正式运行 release。
- 后续黄金链只能从这里学习旧行为，不能恢复旧表、通用 payload 表或双读 fallback。
